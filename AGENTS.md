# AGENTS.md

이 폴더는 한 학기 동안 사용하는 과제 프로젝트 작업 폴더다.

## 구조

- `code/`: 직접 작성한 코드
- `results/`: 실행 로그와 결과 파일

## 실행과 기록

- 코드를 실행한 뒤 화면 출력과 오류를 `results/`에 로그로 남긴다.
- 코드를 고친 뒤에는 로그를 다시 만든다.
- 실행하지 않은 결과를 제출물이나 문서에 적지 않는다.
- 실패한 실행 로그도 삭제하지 않고 원인을 기록한다.

## 작업 원칙

- 파일 경로는 환경에 맞게 설정하고 하드코딩하지 않는다.
- AI가 만든 코드는 실행 전에 직접 읽고 확인한다.
- 문제가 생기면 `gotchas.md`와 `lessons-learned.md`를 갱신한다.
