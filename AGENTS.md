# 과제 프로젝트 규칙

## 작업 위치

- 과제 작성, 코드 수정, 실행, 결과 기록은 항상 이 `my-project` 폴더에서 진행한다.
- GitHub 저장소 `bsy050620/my-project`와 연결된 이 폴더를 과제 프로젝트의 기준 위치로 사용한다.

## 작업 시작 전

- `gotchas.md`와 `lessons-learned.md`를 먼저 읽는다.
- 작업 범위를 정하고 필요한 파일만 수정한다.

## 실행과 증거

- 코드는 `code/`에 둔다.
- 실행할 때 화면 출력과 오류를 함께 `results/`에 로그로 저장한다.
- 코드를 고친 뒤에는 기존 로그를 결과로 재사용하지 말고 다시 실행한다.
- 실행하지 않은 결과를 과제 문서나 제출물에 적지 않는다.

## 코드 규칙

- Python 경로는 가능한 한 `pathlib.Path`를 사용한다.
- 비밀키와 개인정보를 코드나 로그에 넣지 않는다.
- 실패한 실행도 지우지 않고 원인을 기록한다.
